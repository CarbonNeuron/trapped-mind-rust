//! Script parser for the canvas drawing language.
//!
//! Converts a text script into a sequence of drawing commands.

/// Parses a canvas-lang script into a list of commands.
///
/// Currently a placeholder — returns an empty list.
pub fn parse_script(_input: &str) -> Vec<()> {
    vec![]
}
